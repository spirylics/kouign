/**
 * Repository implementations providing concrete adapters for data access.
 * <p>
 * This package contains implementations of outbound ports (repository interfaces) defined in the domain layer. These
 * adapters handle the technical details of data persistence and retrieval.
 * <p>
 * All types are non-null by default unless explicitly annotated with {@code @Nullable}.
 */
@NullMarked
package fr.spirylics.kouign.infrastructure.repository;

import org.jspecify.annotations.NullMarked;
