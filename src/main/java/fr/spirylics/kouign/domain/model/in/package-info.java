/**
 * Inbound ports defining service interfaces for the Model domain.
 * <p>
 * These ports represent the use cases of the application and are implemented by domain services. They are consumed by
 * the application layer (REST controllers).
 * <p>
 * All types are non-null by default unless explicitly annotated with {@code @Nullable}.
 */
@NullMarked
package fr.spirylics.kouign.domain.model.in;

import org.jspecify.annotations.NullMarked;
