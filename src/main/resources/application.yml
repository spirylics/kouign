spring:
  application:
    name: kouign
  threads:
    virtual:
      enabled: true
server:
  port: 9090

management:
  endpoints:
    web:
      exposure:
        include: health
  endpoint:
    health:
      show-details: always
